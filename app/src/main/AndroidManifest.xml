<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Ch.3 Sec.30: continuous motion-sensor monitoring doesn't fit any of Android's predefined
         foreground-service categories (it's not Health Connect biometric data, media, location,
         etc.); "specialUse" is Android's designated category for exactly this case, and unlike
         "health" it doesn't require any body-sensor/Health-Connect permission this app has no
         reason to hold. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- PACKAGE_USAGE_STATS is a special-access permission granted via Settings, not requestable
         at runtime; declaring it here documents the intent even though monitoring itself does
         not require it (Activity classification degrades to Unknown without it, Ch.2 Sec.29). -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".ParallayeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Paralleye"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Debugging aid only, see ParallayeApplication's crash handler. -->
        <activity
            android:name=".CrashReportActivity"
            android:exported="false" />

        <!-- Ch.3 Sec.30, Ch.11 Sec.14/18: continuous background sensor monitoring. -->
        <service
            android:name=".session.MonitoringForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Continuous posture monitoring using the device's motion sensors (accelerometer/gyroscope only)" />
        </service>

        <!-- Ch.11 Sec.19: automatic restart after device reboot, only when already onboarded. -->
        <receiver
            android:name=".session.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
